#include "sort.h"

/**
 * @brief Swaps to values and their corresponding indexes
 * @param p1 The position of the 1st value
 * @param p2 The position of the 2nd value
 * @param data A pointer to the array
 * @param index A pointer to the array of indexes
 */
void swap(unsigned int p1, unsigned int p2, void **data, int *index);

/**
 * @brief ??
 */
void partition(void **data, int *index, unsigned int left, unsigned int right);

void quicksort(int size, void **data, int *index)
{

}

void partition(void **data, int *index, unsigned int left, unsigned int right)
{
    
}

void swap(unsigned int p1, unsigned int p2, void **data, int *index)
{
    
}
